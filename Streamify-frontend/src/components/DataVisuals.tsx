import { useFetch } from "../hooks";

const DataVisuals = () => {
  return (
    <div className="flex items-center justify-evenly px-8 py-4 gap-8">
      DataVisuals
    </div>
  );
};

export default DataVisuals;
